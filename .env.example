# 复制本文件为 .env 并填入真实值；.env 已被 .gitignore 忽略，切勿提交。
#
# CloudBase 静态托管部署凭证
#
# ⚠️ 这里填的是【腾讯云 CAM 密钥】，不是 CloudBase 控制台的 API Key。
#    CloudBase 的 api_key 类型平台尚未开放，用它登录会失败。
#
# 获取方式：腾讯云控制台 → 访问管理 → API 密钥管理 → 新建密钥
#   拿到 SecretId（AKID... 开头）与 SecretKey
#
# 用途：GitHub Actions 部署 / 本地 deploy.ps1
#
# ⚠️ 千万不要把真实值写进仓库。GitHub Actions 请存到：
#    仓库 Settings → Secrets and variables → Actions → New repository secret
#    Secret 名称：TCB_SECRET_ID / TCB_SECRET_KEY

TCB_SECRET_ID=
TCB_SECRET_KEY=
