# 复制本文件为 .env 并填入真实值；.env 已被 .gitignore 忽略，切勿提交。
#
# CloudBase 静态托管部署凭证（API Key，服务端权限）
# 获取方式：CloudBase 控制台 → 环境 → API Key 管理
# 用途：GitHub Actions 部署 / 本地 deploy.ps1
#
# ⚠️ 千万不要把真实值写进仓库。GitHub Actions 请存到：
#    仓库 Settings → Secrets and variables → Actions → New repository secret

CLOUDBASE_APIKEY_ID=
CLOUDBASE_APIKEY=
