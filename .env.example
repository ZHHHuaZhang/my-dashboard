# 复制本文件为 .env 并填入真实值；.env 已被 .gitignore 忽略，切勿提交。
#
# CloudBase 静态托管部署凭证
#
# 用的是【CloudBase 环境级 API Key】，权限被限定在单个环境内，
# 比账号级 CAM 密钥（SecretId/SecretKey）安全得多。
#
# 创建方式：
#   tcb env apikey create github-actions-deploy -e <envId>
#   或在控制台：环境 → API Key 管理
#   注意：令牌明文只在创建时返回一次
#
# 登录命令（参数别写错）：
#   tcb login --cloudbase-api-key <key> -e <envId>
#
# ⚠️ 千万不要把真实值写进仓库。GitHub Actions 请存到：
#    仓库 Settings → Secrets and variables → Actions → New repository secret
#    Secret 名称：CLOUDBASE_API_KEY

CLOUDBASE_API_KEY=
